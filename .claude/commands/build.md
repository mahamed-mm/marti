---
description: Build the project for iOS Simulator.
---

Build this project for iOS Simulator using XcodeBuildMCP.

1. Discover the project: `mcp__xcodebuildmcp__discover_projects`
2. List schemes: `mcp__xcodebuildmcp__list_schemes`
3. Build for simulator: `mcp__xcodebuildmcp__build_sim_name_proj` (default to iPhone 16 unless I've specified otherwise in `CLAUDE.md`)
4. Report the result. If the build fails, identify the failing files and the most likely root cause. Do not auto-fix unless I ask.
